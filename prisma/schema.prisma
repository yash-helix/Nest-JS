datasource db {
    provider = "postgresql"
    url = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client-js"
}

model prismaUsers{
    id String @id
    name String
    createdAt String
    updatedAt String
}